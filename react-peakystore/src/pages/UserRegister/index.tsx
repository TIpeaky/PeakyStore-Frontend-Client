import React, { useState } from "react";
import Form from "../../components/FormRegister";

const UserRegister = () => {
  return (
    <div>
      <Form />
    </div>
  );
};

export default UserRegister;
